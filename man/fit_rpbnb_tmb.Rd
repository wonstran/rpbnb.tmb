% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_rpbnb_tmb.R
\name{fit_rpbnb_tmb}
\alias{fit_rpbnb_tmb}
\title{Fit a bivariate random-parameter negative binomial model (TMB)}
\usage{
fit_rpbnb_tmb(
  formula_1,
  formula_2,
  data,
  random_1 = NULL,
  random_2 = NULL,
  draws = 400L,
  seed = 1234L,
  start = NULL,
  dependence = "famoye",
  control = rpbnb_tmb_control(),
  inference = c("full", "diag", "none"),
  keep = c("postfit", "compact", "full"),
  poisson_1 = FALSE,
  poisson_2 = FALSE
)
}
\arguments{
\item{formula_1, formula_2}{Model formulas for the two count outcomes.}

\item{data}{A data frame.}

\item{random_1, random_2}{Random coefficient specs per equation. NULL (all
fixed), character vector (all Normal), or named list with per-variable
distribution specs (\code{"normal"}, \code{"lognormal"}, \code{"uniform"},
\code{"triangular"}).}

\item{draws}{Number of Halton simulation draws.}

\item{seed}{Random seed for draws.}

\item{start}{Optional starting parameter vector (named or positional).}

\item{dependence}{Dependence structure: "famoye", "independence", or a
\code{copula()} object for copula dependence.}

\item{control}{An object from \code{rpbnb_tmb_control()}.}

\item{inference}{Inference storage: \code{"full"} for a full covariance,
\code{"diag"} for standard errors only, or \code{"none"} to skip Hessian
calculations. In diagonal mode, \code{vcov()} returns \code{NA} for
covariance cross-terms.}

\item{keep}{Retained fit state: \code{"postfit"} keeps data needed for
marginal effects, \code{"compact"} keeps estimates only, and
\code{"full"} also retains the TMB objective and responses. Low-level
diagnostics that access \code{fit$obj} require \code{"full"}.}

\item{poisson_1, poisson_2}{Fit the corresponding margin at its exact Poisson
limit (NB2 dispersion m = 0, held fixed).}
}
\value{
An object of class \code{rpbnb_tmb_fit}. The \code{sdreport} field
is a compact package-owned summary and does not retain a second TMB tape.
}
\description{
Maximum simulated likelihood via TMB (Template Model Builder) with
automatic differentiation. Supports Famoye/Sarmanov and discrete-copula
(Frank, Gaussian, Clayton) dependence.
}
\examples{
\dontrun{
sim <- simulate_rpbnb_tmb(n = 300,
  beta1 = c("(Intercept)" = 0.2, x1 = 0.4),
  beta2 = c("(Intercept)" = 0.1, x1 = -0.3),
  dispersion = c(m1 = 0.4, m2 = 0.5), seed = 1)
fit <- fit_rpbnb_tmb(y1 ~ x1, y2 ~ x1, data = sim$data, draws = 100)
coef(fit)
}
}

# `rpbnb_tmb_dependence_profile()` Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add one exported function returning a confidence interval for the dependence parameter of any fitted `rpbnb_tmb` model, valid at or near a boundary where the delta-method standard error is correctly reported as `NA`.

**Architecture:** Profile the unconstrained working parameter `z_dep` with `TMB::tmbprofile()`, then map the interval endpoints through the family's link. Every link is monotone increasing in `z`, so endpoint-mapping is valid. When the TMB objective is unavailable, degrade to a Wald interval on the working scale mapped through the same link. The four links are extracted into one internal helper that both the new function and the existing `.rpbnb_natural_report()` call, so they cannot drift.

**Tech Stack:** R, TMB, testthat 3, roxygen2.

## Global Constraints

- Spec: `docs/superpowers/specs/2026-07-25-dependence-profile-design.md`.
- `FRANK_THETA_MAX <- 35` (`R/utilities.R:14`) — never inline the literal `35`; reference the constant.
- Famoye logistic epsilon is `1e-6`, matching `R/inference.R` `.rpbnb_natural_report()`.
- Clayton clamp is `exp(pmin(pmax(z, -20), 20))`, matching `clamp_exp()` in `.rpbnb_natural_report()`.
- `map()` returned by `.rpbnb_dependence_link()` takes a **scalar** `z`. `.frank_tau()` is not vectorized.
- Internal (non-exported) objects are reachable from tests without `:::` — the suite runs in the package namespace. See `tests/testthat/test-api-correctness.R:20` using `POISSON_M`.
- Run the suite with:
  ```
  $env:R_LIBS_USER="C:/Users/litabook/AppData/Local/R/win-library/4.5"
  & 'C:\Program Files\R\R-4.5.1\bin\Rscript.exe' -e "devtools::test()"
  ```
- Baseline before this work: `[ FAIL 2 | WARN 0 | SKIP 0 | PASS 326 ]`. The 2 failures are pre-existing in `test-against-rpbnb.R` and caused by the sibling `rpbnb` package shipping CSVs in `data/`. **Do not treat them as regressions, and do not attempt to fix them here.** Every task below must leave the failure count at exactly 2.
- To run one file: `devtools::test(filter = "dependence-profile")`.

## File Structure

| File | Responsibility |
|---|---|
| `R/utilities.R` | Add `.resolve_family_code()` next to the other shared constants/helpers. |
| `R/fit_rpbnb_tmb.R` | Replace the inline dependence `switch` with the new helper; add a `@seealso` cross-reference. |
| `R/inference.R` | Add `.rpbnb_dependence_link()`, `.dependence_profile_ci()`, and the exported `rpbnb_tmb_dependence_profile()`; refactor `.rpbnb_natural_report()` to source its values from the shared link. |
| `R/rpbnb.tmb-package.R` | Add `@importFrom stats confint qnorm setNames`. |
| `tests/testthat/test-api-correctness.R` | Add the family-code resolution test. |
| `tests/testthat/test-dependence-profile.R` | New. All profile/Wald behaviour. |
| `NAMESPACE`, `man/` | Regenerated by roxygen. |

---

### Task 1: Shared family-code resolution

**Files:**
- Modify: `R/utilities.R` (append helper after `.resolve_start()`, which ends at line 309)
- Modify: `R/fit_rpbnb_tmb.R:98-111`
- Test: `tests/testthat/test-api-correctness.R` (append)

**Interfaces:**
- Consumes: nothing.
- Produces: `.resolve_family_code(dependence)` returning `integer(1)`: `-1L` independence, `0L` famoye, `1L` frank, `2L` normal, `3L` kimeldorf. Errors on anything else.

- [ ] **Step 1: Write the failing test**

Append to `tests/testthat/test-api-correctness.R`:

```r
test_that("dependence specifications resolve to one shared family code", {
  expect_identical(.resolve_family_code("independence"), -1L)
  expect_identical(.resolve_family_code("famoye"), 0L)
  expect_identical(.resolve_family_code(copula("frank")), 1L)
  expect_identical(.resolve_family_code(copula("normal")), 2L)
  expect_identical(.resolve_family_code(copula("kimeldorf")), 3L)
  # "gaussian" is not a valid dependence string; only copula("normal") is.
  expect_error(.resolve_family_code("gaussian"), "must be")
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `devtools::test(filter = "api-correctness")`
Expected: FAIL, `could not find function ".resolve_family_code"`.

- [ ] **Step 3: Write minimal implementation**

Append to `R/utilities.R`:

```r
#' Map a dependence specification to its family code
#'
#' The single definition shared by the fitter and by
#' \code{rpbnb_tmb_dependence_profile()}. Keeping one copy is the point: a
#' second inline \code{switch} would silently disagree the moment a family is
#' added.
#' @keywords internal
#' @noRd
.resolve_family_code <- function(dependence) {
  if (inherits(dependence, "rpbnb_copula")) {
    return(switch(dependence$family,
                  frank = 1L, normal = 2L, kimeldorf = 3L))
  }
  if (identical(dependence, "famoye")) return(0L)
  if (identical(dependence, "independence")) return(-1L)
  stop("`dependence` must be \"famoye\", \"independence\", or copula().",
       call. = FALSE)
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `devtools::test(filter = "api-correctness")`
Expected: PASS.

- [ ] **Step 5: Replace the inline switch in the fitter**

In `R/fit_rpbnb_tmb.R`, replace lines 98-111 exactly:

```r
  # Resolve dependence
  if (inherits(dependence, "rpbnb_copula")) {
    if (isTRUE(poisson_1) || isTRUE(poisson_2)) {
      stop("Poisson-limit margins not supported with copula dependence.")
    }
    family_code <- switch(dependence$family,
      frank = 1L, normal = 2L, kimeldorf = 3L)
  } else if (identical(dependence, "famoye")) {
    family_code <- 0L
  } else if (identical(dependence, "independence")) {
    family_code <- -1L
  } else {
    stop("`dependence` must be \"famoye\", \"independence\", or copula().")
  }
```

with:

```r
  # Resolve dependence
  family_code <- .resolve_family_code(dependence)
  # Codes 1-3 are exactly the copula families.
  if (family_code >= 1L && (isTRUE(poisson_1) || isTRUE(poisson_2))) {
    stop("Poisson-limit margins not supported with copula dependence.")
  }
```

Note two intentional behaviour changes: the invalid-`dependence` error now prints without a `call.` prefix, and an invalid `dependence` combined with `poisson_1 = TRUE` now reports the dependence problem first. The message text is unchanged, so message-matching tests still pass.

- [ ] **Step 6: Run the full suite**

Run: `devtools::test()`
Expected: `[ FAIL 2 | PASS 327+ ]` — the same 2 pre-existing `test-against-rpbnb.R` failures and nothing new.

- [ ] **Step 7: Commit**

```bash
git add R/utilities.R R/fit_rpbnb_tmb.R tests/testthat/test-api-correctness.R
git commit -m "refactor: extract .resolve_family_code() as the single dependence mapping"
```

---

### Task 2: Shared dependence links

**Files:**
- Modify: `R/inference.R` (add helper above `.rpbnb_natural_report()`, which starts at line 4; then refactor its family branches at lines 83-146)
- Test: `tests/testthat/test-dependence-profile.R` (create)

**Interfaces:**
- Consumes: `.resolve_family_code()` (Task 1), `FRANK_THETA_MAX`, `.frank_tau()`.
- Produces: `.rpbnb_dependence_link(family_code, lamLo = NA_real_, lamHi = NA_real_)` returning `NULL` when `family_code < 0L`, else a list with:
  - `names` — `character` vector of reported quantity names in output order: `"lam"`; `c("theta", "tau")`; `c("rho", "tau")`; `c("theta", "tau")`.
  - `map` — `function(z)` taking a **scalar** numeric and returning a named numeric vector whose names equal `names`.

- [ ] **Step 1: Write the failing test**

Create `tests/testthat/test-dependence-profile.R`:

```r
test_that("the shared dependence link reproduces the reported natural values", {
  z <- 0.37
  lo <- -0.4
  hi <- 0.9
  coefs <- c(log_m1 = log(0.5), log_m2 = log(0.4), z_dep = z)

  for (fc in 0:3) {
    link <- .rpbnb_dependence_link(fc, lamLo = lo, lamHi = hi)
    report <- .rpbnb_natural_report(coefs, family_code = fc,
                                    lamLo = lo, lamHi = hi)
    values <- link$map(z)

    expect_identical(names(values), link$names, info = paste("family", fc))
    for (nm in link$names) {
      expect_equal(report[[nm]]$value, unname(values[[nm]]),
                   info = paste("family", fc, nm))
    }
  }
})

test_that("independence has no dependence link", {
  expect_null(.rpbnb_dependence_link(-1L))
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `devtools::test(filter = "dependence-profile")`
Expected: FAIL, `could not find function ".rpbnb_dependence_link"`.

- [ ] **Step 3: Add the helper**

Insert into `R/inference.R` immediately above the `#' Natural-scale report values` block that starts at line 1:

```r
#' Monotone links from the working dependence parameter to natural scale
#'
#' Every family's link is monotone increasing in \code{z}, which is what makes
#' it legitimate to map the endpoints of a working-scale interval through it.
#' \code{.rpbnb_natural_report()} and \code{rpbnb_tmb_dependence_profile()} both
#' read their values from here so the two cannot disagree.
#'
#' \code{map} takes a scalar: \code{.frank_tau()} is not vectorized.
#' @keywords internal
#' @noRd
.rpbnb_dependence_link <- function(family_code,
                                   lamLo = NA_real_, lamHi = NA_real_) {
  if (family_code < 0L) return(NULL)
  if (family_code == 0L) {
    eps <- 1e-6
    span <- lamHi - lamLo
    return(list(
      names = "lam",
      map = function(z) {
        stopifnot(length(z) == 1L)
        c(lam = lamLo + span * (eps + (1 - 2 * eps) * stats::plogis(z)))
      }
    ))
  }
  if (family_code == 1L) {
    return(list(
      names = c("theta", "tau"),
      map = function(z) {
        stopifnot(length(z) == 1L)
        theta <- FRANK_THETA_MAX * tanh(z / FRANK_THETA_MAX)
        c(theta = theta, tau = .frank_tau(theta))
      }
    ))
  }
  if (family_code == 2L) {
    return(list(
      names = c("rho", "tau"),
      map = function(z) {
        stopifnot(length(z) == 1L)
        rho <- tanh(z)
        c(rho = rho, tau = 2 / pi * asin(rho))
      }
    ))
  }
  list(
    names = c("theta", "tau"),
    map = function(z) {
      stopifnot(length(z) == 1L)
      theta <- exp(pmin(pmax(z, -20), 20))
      c(theta = theta, tau = theta / (theta + 2))
    }
  )
}
```

- [ ] **Step 4: Route `.rpbnb_natural_report()` values through the link**

In `R/inference.R`, immediately after `z <- unname(coef["z_dep"])` (line 82), insert:

```r
  link_values <- .rpbnb_dependence_link(
    family_code, lamLo = lamLo, lamHi = lamHi
  )$map(z)
```

Then in each family branch replace only the value assignment, leaving every derivative and boundary expression untouched:

- Family `0L`: replace `value <- lamLo + span * (eps + (1 - 2 * eps) * sig)` with `value <- link_values[["lam"]]`. Keep the `eps`, `sig`, and `span` lines — the derivative still needs them.
- Family `1L`: replace `theta <- FRANK_THETA_MAX * tanh(z / FRANK_THETA_MAX)` with `theta <- link_values[["theta"]]`, and `tau <- .frank_tau(theta)` with `tau <- link_values[["tau"]]`.
- Family `2L`: replace `rho <- tanh(z)` with `rho <- link_values[["rho"]]`, and `tau <- 2 / pi * asin(rho)` with `tau <- link_values[["tau"]]`.
- Family `3L`: replace `theta <- clamp_exp(z)` with `theta <- link_values[["theta"]]`, and `tau <- theta / (theta + 2)` with `tau <- link_values[["tau"]]`.

- [ ] **Step 5: Run tests to verify they pass**

Run: `devtools::test(filter = "dependence-profile")`
Expected: PASS, 2 tests.

- [ ] **Step 6: Run the full suite**

Run: `devtools::test()`
Expected: `[ FAIL 2 | PASS 329+ ]`. The pre-existing boundary tests in `test-inference-memory.R` exercise `.rpbnb_natural_report()` heavily — if any of them break, the refactor changed a value and must be corrected, not the test.

- [ ] **Step 7: Commit**

```bash
git add R/inference.R tests/testthat/test-dependence-profile.R
git commit -m "refactor: source natural-scale values from one shared dependence link"
```

---

### Task 3: Profile-likelihood interval on the working scale

**Files:**
- Modify: `R/inference.R` (append `.dependence_profile_ci()` after `.rpbnb_dependence_link()`)
- Modify: `R/rpbnb.tmb-package.R`
- Test: `tests/testthat/test-dependence-profile.R` (append)

**Interfaces:**
- Consumes: `fit$obj`, `fit$optimizer$par`.
- Produces: `.dependence_profile_ci(fit, level, ...)` returning either `NULL` (profile not attemptable — caller must fall back to Wald) or a length-2 unnamed numeric of **working-scale** endpoints, either of which may be `NA`, with the raw profile in `attr(, "profile")`. `NULL` and `c(NA, NA)` are deliberately different: `NULL` means "could not run", `c(NA, NA)` means "ran and did not bracket".

- [ ] **Step 1: Write the failing test**

Append to `tests/testthat/test-dependence-profile.R`:

```r
dp_test_data <- function(n = 200L) {
  set.seed(250725)
  x <- stats::runif(n, -1, 1)
  shared <- stats::rnorm(n, 0, 0.3)
  data.frame(
    y1 = stats::rpois(n, exp(0.4 + 0.3 * x + shared)),
    y2 = stats::rpois(n, exp(0.3 - 0.2 * x + shared)),
    x  = x
  )
}

test_that("the working-scale profile brackets the working estimate", {
  fit <- fit_rpbnb_tmb(
    y1 ~ x, y2 ~ x, dp_test_data(),
    dependence = copula("normal"), draws = 1L, keep = "full"
  )

  ci <- .dependence_profile_ci(fit, level = 0.95)

  expect_false(is.null(ci))
  expect_length(as.numeric(ci), 2L)
  expect_false(is.null(attr(ci, "profile")))
  z_hat <- unname(coef(fit)[["z_dep"]])
  expect_lt(as.numeric(ci)[1], z_hat)
  expect_gt(as.numeric(ci)[2], z_hat)
})

test_that("a fit without the TMB objective yields NULL, not an error", {
  fit <- fit_rpbnb_tmb(
    y1 ~ x, y2 ~ x, dp_test_data(),
    dependence = copula("normal"), draws = 1L, keep = "postfit"
  )

  expect_null(fit$obj)
  expect_null(.dependence_profile_ci(fit, level = 0.95))
})
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `devtools::test(filter = "dependence-profile")`
Expected: FAIL, `could not find function ".dependence_profile_ci"`.

- [ ] **Step 3: Add the import**

In `R/rpbnb.tmb-package.R`, add to the existing `@importFrom stats` line(s):

```r
#' @importFrom stats confint qnorm setNames
```

`confint()` must be the `stats` generic so TMB's registered `confint.tmbprofile` method dispatches.

- [ ] **Step 4: Write the implementation**

Append to `R/inference.R`:

```r
#' Profile-likelihood interval for the working dependence parameter
#'
#' Returns working-scale endpoints, or \code{NULL} when no profile can be
#' attempted so the caller can fall back to a Wald interval.
#' @keywords internal
#' @noRd
.dependence_profile_ci <- function(fit, level, ...) {
  obj <- fit$obj
  if (is.null(obj)) return(NULL)

  # Presence is not liveness: a fit round-tripped through saveRDS() keeps the
  # obj field but its external pointer is dead.
  probe <- try(obj$fn(fit$optimizer$par), silent = TRUE)
  if (inherits(probe, "try-error") ||
      length(probe) != 1L || !is.finite(probe)) {
    return(NULL)
  }

  # tmbprofile() profiles from last.par.best, not obj$par. Restore it rather
  # than trusting whatever last touched the objective.
  obj$env$last.par.best <- fit$optimizer$par

  dots <- list(...)
  run <- function(extra) {
    args <- c(list(obj, "z_dep", trace = FALSE), dots, extra)
    result <- try(do.call(TMB::tmbprofile, args), silent = TRUE)
    if (inherits(result, "try-error")) NULL else result
  }

  profile <- run(NULL)
  if (is.null(profile)) return(NULL)
  endpoints <- as.numeric(stats::confint(profile, level = level))

  # confint.tmbprofile() locates endpoints with approx(), so an uncrossed
  # profile yields NA rather than an infinite bound. Widening ytol searches
  # further before giving up.
  if (anyNA(endpoints) && !("ytol" %in% names(dots))) {
    wider <- run(list(ytol = 10))
    if (!is.null(wider)) {
      wider_endpoints <- as.numeric(stats::confint(wider, level = level))
      if (sum(is.na(wider_endpoints)) < sum(is.na(endpoints))) {
        profile <- wider
        endpoints <- wider_endpoints
      }
    }
  }

  structure(endpoints, profile = profile)
}
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `devtools::test(filter = "dependence-profile")`
Expected: PASS, 4 tests.

- [ ] **Step 6: Commit**

```bash
git add R/inference.R R/rpbnb.tmb-package.R tests/testthat/test-dependence-profile.R
git commit -m "feat: add working-scale profile-likelihood interval for z_dep"
```

---

### Task 4: Exported `rpbnb_tmb_dependence_profile()`

**Files:**
- Modify: `R/inference.R` (append the exported function)
- Test: `tests/testthat/test-dependence-profile.R` (append)

**Interfaces:**
- Consumes: `.resolve_family_code()` (Task 1), `.rpbnb_dependence_link()` (Task 2), `.dependence_profile_ci()` (Task 3), `fit$se`, `fit$coef`, `fit$lambda_bounds`.
- Produces: `rpbnb_tmb_dependence_profile(fit, level = 0.95, method = c("profile", "wald"), ...)` returning a `data.frame` with columns `parameter` (character), `estimate`, `lower`, `upper`, `level` (numeric), `method` (character), and `attr(, "profile")`.

- [ ] **Step 1: Write the failing tests**

Append to `tests/testthat/test-dependence-profile.R`:

```r
test_that("the profile interval brackets the estimate on the natural scale", {
  fit <- fit_rpbnb_tmb(
    y1 ~ x, y2 ~ x, dp_test_data(),
    dependence = copula("normal"), draws = 1L, keep = "full"
  )

  pr <- rpbnb_tmb_dependence_profile(fit)

  expect_identical(pr$parameter, c("rho", "tau"))
  expect_identical(unique(pr$method), "profile")
  expect_true(all(pr$lower < pr$estimate))
  expect_true(all(pr$estimate < pr$upper))
  expect_true(all(pr$level == 0.95))
})

test_that("a wider level nests the narrower one", {
  fit <- fit_rpbnb_tmb(
    y1 ~ x, y2 ~ x, dp_test_data(),
    dependence = copula("normal"), draws = 1L, keep = "full"
  )

  narrow <- rpbnb_tmb_dependence_profile(fit, level = 0.95)
  wide   <- rpbnb_tmb_dependence_profile(fit, level = 0.99)

  expect_true(all(wide$lower <= narrow$lower))
  expect_true(all(wide$upper >= narrow$upper))
})

test_that("Famoye endpoints stay inside the frozen lambda bounds", {
  fit <- fit_rpbnb_tmb(
    y1 ~ x, y2 ~ x, dp_test_data(),
    dependence = "famoye", draws = 1L, keep = "full"
  )

  pr <- suppressWarnings(rpbnb_tmb_dependence_profile(fit))
  bounds <- fit$lambda_bounds

  expect_identical(pr$parameter, "lam")
  # Holds whether or not the optimum pinned: a pinned fit returns NA endpoints,
  # and every finite endpoint must lie in the box.
  ends <- c(pr$lower, pr$upper)
  ends <- ends[is.finite(ends)]
  expect_true(all(ends >= bounds[["lower"]]))
  expect_true(all(ends <= bounds[["upper"]]))
})

test_that("a fit without the TMB objective warns and degrades to Wald", {
  fit <- fit_rpbnb_tmb(
    y1 ~ x, y2 ~ x, dp_test_data(),
    dependence = copula("normal"), draws = 1L, keep = "postfit"
  )

  expect_warning(
    pr <- rpbnb_tmb_dependence_profile(fit),
    "falling back to a Wald interval"
  )
  expect_identical(unique(pr$method), "wald")
  expect_true(all(is.finite(pr$lower)))
  expect_true(all(pr$lower < pr$estimate))
  expect_null(attr(pr, "profile"))
})

test_that("a reloaded fit degrades rather than erroring on a dead pointer", {
  fit <- fit_rpbnb_tmb(
    y1 ~ x, y2 ~ x, dp_test_data(),
    dependence = copula("normal"), draws = 1L, keep = "full"
  )
  path <- tempfile(fileext = ".rds")
  saveRDS(fit, path)
  reloaded <- readRDS(path)
  on.exit(unlink(path), add = TRUE)

  expect_warning(
    pr <- rpbnb_tmb_dependence_profile(reloaded),
    "falling back to a Wald interval"
  )
  expect_identical(unique(pr$method), "wald")
})

test_that("independence has nothing to profile", {
  fit <- fit_rpbnb_tmb(
    y1 ~ x, y2 ~ x, dp_test_data(),
    dependence = "independence", draws = 1L, keep = "full"
  )

  expect_error(rpbnb_tmb_dependence_profile(fit), "nothing to profile")
})

test_that("level and fit are validated", {
  fit <- fit_rpbnb_tmb(
    y1 ~ x, y2 ~ x, dp_test_data(),
    dependence = copula("normal"), draws = 1L, keep = "full"
  )

  expect_error(rpbnb_tmb_dependence_profile(fit, level = 1), "between 0 and 1")
  expect_error(rpbnb_tmb_dependence_profile(fit, level = c(0.9, 0.95)),
               "between 0 and 1")
  expect_error(rpbnb_tmb_dependence_profile(list()), "rpbnb_tmb_fit")
})

test_that('inference = "none" returns the estimate with NA endpoints', {
  fit <- fit_rpbnb_tmb(
    y1 ~ x, y2 ~ x, dp_test_data(),
    dependence = copula("normal"), draws = 1L,
    keep = "postfit", inference = "none"
  )

  pr <- suppressWarnings(rpbnb_tmb_dependence_profile(fit))

  expect_true(all(is.finite(pr$estimate)))
  expect_true(all(is.na(pr$lower)))
  expect_true(all(is.na(pr$upper)))
})

test_that("the reported estimate matches the fit's own natural value", {
  for (dep in list("famoye", copula("frank"))) {
    fit <- fit_rpbnb_tmb(
      y1 ~ x, y2 ~ x, dp_test_data(),
      dependence = dep, draws = 1L, keep = "full"
    )
    pr <- suppressWarnings(rpbnb_tmb_dependence_profile(fit))
    reported <- summary(fit$sdreport, "report")

    for (nm in pr$parameter) {
      expect_equal(
        pr$estimate[pr$parameter == nm],
        unname(reported[nm, "Estimate"]),
        info = nm
      )
    }
  }
})
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `devtools::test(filter = "dependence-profile")`
Expected: FAIL, `could not find function "rpbnb_tmb_dependence_profile"`.

- [ ] **Step 3: Write the implementation**

Append to `R/inference.R`:

```r
#' Confidence interval for a fitted dependence parameter
#'
#' Returns a profile-likelihood interval for the dependence parameter of a
#' fitted model, computed on the unconstrained working scale and mapped through
#' the family's monotone link. This is the tool to reach for when
#' \code{summary()} reports \code{NA} for the dependence standard error: at a
#' boundary the delta-method derivative collapses, so
#' \eqn{SE = |d\theta/dz| \cdot SE(z)} is a \eqn{0 \times \infty} product and no
#' symmetric standard error exists. A profile interval needs no derivative.
#'
#' For Famoye this is the usual situation, because the admissible lambda
#' interval is frozen at the starting values (see \code{lambda_bounds} in
#' \code{\link{fit_rpbnb_tmb}}). An interval around a pinned estimate is still
#' an interval around an artefact -- widen the box first by refitting from
#' better starting values.
#'
#' @param fit An object of class \code{rpbnb_tmb_fit}.
#' @param level Coverage level; one number strictly between 0 and 1.
#' @param method \code{"profile"} (default) for a profile-likelihood interval,
#'   or \code{"wald"} for a Wald interval on the working scale mapped through
#'   the same link. \code{"profile"} requires the TMB objective, retained only
#'   under \code{keep = "full"}; when it is unavailable this degrades to
#'   \code{"wald"} with a warning rather than failing.
#' @param ... Passed to \code{\link[TMB]{tmbprofile}}, e.g. \code{ytol} or
#'   \code{parm.range}.
#' @return A data frame with one row per reported dependence quantity and
#'   columns \code{parameter}, \code{estimate}, \code{lower}, \code{upper},
#'   \code{level}, and \code{method} -- the method actually used, which may
#'   differ from the one requested. The raw profile, when one was computed, is
#'   attached as \code{attr(, "profile")} so it can be plotted.
#' @seealso \code{\link{fit_rpbnb_tmb}}
#' @export
#' @examples
#' \dontrun{
#' fit <- fit_rpbnb_tmb(y1 ~ x, y2 ~ x, data = d,
#'                      dependence = "famoye", keep = "full")
#' rpbnb_tmb_dependence_profile(fit)
#' plot(attr(rpbnb_tmb_dependence_profile(fit), "profile"))
#' }
rpbnb_tmb_dependence_profile <- function(fit, level = 0.95,
                                        method = c("profile", "wald"), ...) {
  if (!inherits(fit, "rpbnb_tmb_fit")) {
    stop("`fit` must be an object of class \"rpbnb_tmb_fit\".", call. = FALSE)
  }
  if (length(level) != 1L || !is.numeric(level) || is.na(level) ||
      level <= 0 || level >= 1) {
    stop("`level` must be one number strictly between 0 and 1.", call. = FALSE)
  }
  method <- match.arg(method)

  family_code <- .resolve_family_code(fit$dependence)
  if (family_code < 0L) {
    stop("An independence fit estimates no dependence parameter, so there is ",
         "nothing to profile.", call. = FALSE)
  }

  bounds <- fit$lambda_bounds
  link <- .rpbnb_dependence_link(
    family_code,
    lamLo = if (is.null(bounds)) NA_real_ else bounds[["lower"]],
    lamHi = if (is.null(bounds)) NA_real_ else bounds[["upper"]]
  )
  z_hat <- unname(fit$coef[["z_dep"]])

  profile <- NULL
  endpoints <- NULL
  if (identical(method, "profile")) {
    endpoints <- .dependence_profile_ci(fit, level = level, ...)
    if (is.null(endpoints)) {
      warning(
        "The TMB objective is unavailable, so no profile can be computed; ",
        "falling back to a Wald interval on the working scale. Refit with ",
        "keep = \"full\" for a profile interval.",
        call. = FALSE
      )
      method <- "wald"
    } else {
      profile <- attr(endpoints, "profile")
      endpoints <- as.numeric(endpoints)
    }
  }
  if (identical(method, "wald")) {
    se_z <- unname(fit$se[["z_dep"]])
    if (!is.finite(se_z)) {
      warning(
        "No standard error is available for z_dep, so the interval endpoints ",
        "are NA. Refit with inference = \"full\" or \"diag\".",
        call. = FALSE
      )
      endpoints <- c(NA_real_, NA_real_)
    } else {
      half <- stats::qnorm(1 - (1 - level) / 2) * se_z
      endpoints <- c(z_hat - half, z_hat + half)
    }
  }

  # Every link is monotone increasing, so the lower working endpoint maps to the
  # lower natural endpoint.
  map_or_na <- function(z) {
    if (!is.finite(z)) {
      stats::setNames(rep(NA_real_, length(link$names)), link$names)
    } else {
      link$map(z)
    }
  }
  estimate <- map_or_na(z_hat)
  lower <- map_or_na(endpoints[1])
  upper <- map_or_na(endpoints[2])

  out <- data.frame(
    parameter = link$names,
    estimate  = unname(estimate[link$names]),
    lower     = unname(lower[link$names]),
    upper     = unname(upper[link$names]),
    level     = level,
    method    = method,
    stringsAsFactors = FALSE
  )

  # Only for the profile path: the Wald path already warned about its own NAs.
  if (identical(method, "profile")) {
    if (anyNA(out$lower)) {
      warning("The profile did not cross the cutoff on the lower side, so ",
              "the lower endpoint is NA; the likelihood is flat in that ",
              "direction.", call. = FALSE)
    }
    if (anyNA(out$upper)) {
      warning("The profile did not cross the cutoff on the upper side, so ",
              "the upper endpoint is NA; the likelihood is flat in that ",
              "direction.", call. = FALSE)
    }
  }

  attr(out, "profile") <- profile
  out
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `devtools::test(filter = "dependence-profile")`
Expected: PASS, 13 tests.

If the Famoye test's `expect_identical(pr$parameter, "lam")` passes but the `summary(fit$sdreport, "report")` lookup in the last test fails for Famoye, the cause is a pinned estimate whose `sdreport` row is `NA` for `Std. Error` only — `Estimate` is still populated, so the comparison should hold. If `lam` is absent from the report rownames entirely, that is a real finding: report it rather than deleting the assertion.

- [ ] **Step 5: Run the full suite**

Run: `devtools::test()`
Expected: `[ FAIL 2 | PASS 342+ ]`.

- [ ] **Step 6: Commit**

```bash
git add R/inference.R tests/testthat/test-dependence-profile.R
git commit -m "feat: add rpbnb_tmb_dependence_profile() with Wald fallback"
```

---

### Task 5: Documentation and cross-references

**Files:**
- Modify: `R/fit_rpbnb_tmb.R:48-54` (the `lambda_bounds` `@return` text)
- Modify: `R/inference.R` (the dependence boundary warning's remedy text, at the `warning()` call whose message begins `"Dependence estimate(s) "`)
- Regenerate: `NAMESPACE`, `man/`

**Interfaces:**
- Consumes: `rpbnb_tmb_dependence_profile()` (Task 4).
- Produces: no new code interfaces.

- [ ] **Step 1: Point the `lambda_bounds` documentation at the new function**

In `R/fit_rpbnb_tmb.R`, the `@return` text currently ends:

```
#'   therefore an artefact of the starting values rather than a property of the
#'   data, which is why the field is exposed.
```

Append to that sentence:

```
#'   therefore an artefact of the starting values rather than a property of the
#'   data, which is why the field is exposed. For an interval that remains valid
#'   at such an optimum, see
#'   \code{\link{rpbnb_tmb_dependence_profile}}.
```

- [ ] **Step 2: Point the boundary warning at the new function**

In `R/inference.R`, in the `warning()` whose message begins `"Dependence estimate(s) "`, replace this final clause:

```r
      "Refit from different starting values, or use a dependence family whose ",
      "range covers the association in these data.",
```

with:

```r
      "Refit from different starting values, or use a dependence family whose ",
      "range covers the association in these data. ",
      "rpbnb_tmb_dependence_profile() gives an interval that stays valid here.",
```

- [ ] **Step 3: Fix the run-on `cause` phrasing while here**

In `R/inference.R`, in the same `cause <- switch(...)`, replace:

```r
      "0" = "the Famoye bounds frozen at the starting values",
```

with:

```r
      "0" = "the Famoye bounds, which are frozen at the starting values",
```

This is the clause that made the assembled sentence read as a run-on. Any test asserting on this text must be updated to match, not reverted.

- [ ] **Step 4: Regenerate documentation**

Run:

```
& 'C:\Program Files\R\R-4.5.1\bin\Rscript.exe' -e "devtools::document()"
```

Expected: `NAMESPACE` gains `export(rpbnb_tmb_dependence_profile)` and the `stats` imports; `man/rpbnb_tmb_dependence_profile.Rd` is created.

- [ ] **Step 5: Run the full suite**

Run: `devtools::test()`
Expected: `[ FAIL 2 | PASS 342+ ]`. If a `test-inference-memory.R` warning-text assertion fails, update it to the new wording from Steps 2-3.

- [ ] **Step 6: Check the package builds clean**

Run:

```
& 'C:\Program Files\R\R-4.5.1\bin\Rscript.exe' -e "devtools::check(document = FALSE, args = '--no-tests', quiet = TRUE)"
```

Expected: no ERRORs and no WARNINGs from missing documentation or undeclared imports. NOTEs about the pre-existing `data/` layout are acceptable.

- [ ] **Step 7: Commit**

```bash
git add R/ man/ NAMESPACE tests/
git commit -m "docs: cross-reference rpbnb_tmb_dependence_profile() from the boundary warning"
```

---

## Self-Review

**Spec coverage.** Every spec section maps to a task: `.resolve_family_code()` → Task 1; `.rpbnb_dependence_link()` and the anti-drift refactor → Task 2; `.dependence_profile_ci()`, the liveness probe, `last.par.best` restoration, and the `ytol` retry → Task 3; the exported function, return shape, Wald path, and all seven error-handling rows → Task 4; documentation and cross-references → Task 5. All nine spec tests appear: brackets (Task 4 test 1), bounds containment (test 3), level nesting (test 2), Wald fallback (test 4), stale pointer (test 5), independence (test 6), Frank rows (test 9, via the `copula("frank")` loop asserting `parameter` against the sdreport), anti-drift (Task 2 test 1 at the internal level and Task 4 test 9 end-to-end), `inference = "none"` (test 8).

**Deviation from the spec, recorded deliberately.** The spec's test 7 asserted Frank's `parameter` column is exactly `c("theta", "tau")`. That assertion lives in Task 2's `expect_identical(names(values), link$names)` plus Task 4's final loop rather than as a separate test, because a standalone Frank fit adds a slow test for one already-covered assertion. Task 5 Step 3 also fixes the run-on `cause` phrasing, which the spec noted as a readability nit but did not schedule; it is one line and belongs with the other wording change in the same `warning()`.

**Placeholder scan.** No TBD/TODO, no "add validation", no "similar to Task N". Every code step carries complete code.

**Type consistency.** `.resolve_family_code` returns `integer`, compared with `expect_identical` against `-1L`/`0L`/`1L`/`2L`/`3L` throughout. `.rpbnb_dependence_link` returns `list(names, map)` and every consumer uses `link$names` and `link$map` — no other field names appear. `.dependence_profile_ci` returns `NULL` or a length-2 numeric with a `"profile"` attribute; Task 4 checks `is.null()` first and only then reads `attr(, "profile")`. `map()` takes a scalar everywhere, guarded by `stopifnot(length(z) == 1L)` and called only via `map_or_na()` on single values.

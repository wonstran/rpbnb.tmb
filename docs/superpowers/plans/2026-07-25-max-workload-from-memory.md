# `rpbnb_tmb_max_workload()` Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add an exported `rpbnb_tmb_max_workload()` that computes the TMB tape-memory guard's budget from an explicit GiB figure or from auto-detected available memory, and make `rpbnb_tmb_control()`'s `max_workload` default call it instead of the fixed calibration constant.

**Architecture:** `.detect_available_memory_gib()` is a new internal, per-OS, never-erroring memory probe (`/proc/meminfo` on Linux, `vm_stat` on macOS, `wmic` on Windows; `NA_real_` on any failure). `rpbnb_tmb_max_workload()` applies the same `gib * 1024^3 / peak_bytes_per_unit` arithmetic `.calibration_default_workload()` already uses, either to a caller-supplied `budget_gib` (trusted as-is) or to 80% of detected available memory (falling back to the fixed calibration constant, with a warning, when detection fails). `rpbnb_tmb_control()`'s default argument changes to call the new function.

**Tech Stack:** R, base-R-only OS detection (no new package dependency), testthat 3's `local_mocked_bindings()` for deterministic tests, roxygen2.

## Global Constraints

- Spec: `docs/superpowers/specs/2026-07-25-max-workload-from-memory-design.md`.
- `TAPE_CALIBRATION$peak_bytes_per_unit = 12083`, `TAPE_CALIBRATION$budget_gib = 8` (`R/utilities.R:31-42`) — reference these constants, never inline the literals.
- `fraction` applies **only** to auto-detected memory. An explicit `budget_gib` is never discounted.
- `.detect_available_memory_gib()` must never throw — every `system2()`/file-read path is wrapped so failure yields `NA_real_`.
- A default argument that can error breaks every caller who doesn't override it — `rpbnb_tmb_max_workload()` itself must likewise never error on the auto-detect path; it warns and falls back instead.
- Existing validation style (`R/utilities.R:377-399`): check `length == 1`, `is.numeric`, `!is.na`, range, with `stop("...", call. = FALSE)` ending in a period. New validation must match this style.
- testthat is already required at `>= 3.1.5` (`DESCRIPTION`); `local_mocked_bindings()` has been available since 3.1.4, no version bump needed.
- Run the suite with:
  ```
  $env:R_LIBS_USER="C:/Users/litabook/AppData/Local/R/win-library/4.5"
  & 'C:\Program Files\R\R-4.5.1\bin\Rscript.exe' -e "devtools::test()"
  ```
- Baseline before this work: `[ FAIL 2 | WARN 0 | SKIP 0 | PASS 391 ]`. The 2 failures are pre-existing in `test-against-rpbnb.R`, caused by the sibling `rpbnb` package shipping CSVs in `data/`. Not yours — every task below must leave the failure count at exactly 2.
- To run one file: `devtools::test(filter = "max-workload")` or `devtools::test(filter = "parallel")`.

## File Structure

| File | Responsibility |
|---|---|
| `R/utilities.R` | Add `.detect_available_memory_gib()` and `rpbnb_tmb_max_workload()` near `.calibration_default_workload()`; change `rpbnb_tmb_control()`'s default; extend `.calibration_doc()`. |
| `tests/testthat/test-max-workload.R` | New. All `rpbnb_tmb_max_workload()` and `.detect_available_memory_gib()` behavior. |
| `tests/testthat/test-parallel.R` | Rewrite the two tests whose contract this change breaks (lines 210-244). |
| `NAMESPACE`, `man/` | Regenerated by roxygen. |

---

### Task 1: `.detect_available_memory_gib()`

**Files:**
- Modify: `R/utilities.R` (insert after `.calibration_default_workload()`, which ends at line 107)
- Test: `tests/testthat/test-max-workload.R` (create)

**Interfaces:**
- Consumes: nothing.
- Produces: `.detect_available_memory_gib()` returning one non-negative finite `double`, or `NA_real_` on any failure. Never errors.

- [ ] **Step 1: Write the failing test**

Create `tests/testthat/test-max-workload.R`:

```r
test_that(".detect_available_memory_gib() never errors and returns a sane value", {
  result <- .detect_available_memory_gib()

  expect_length(result, 1L)
  expect_true(is.numeric(result))
  # Either a real reading or an explicit "could not detect" signal -- never
  # NaN, never negative, never an error propagating out of this function.
  expect_true(is.na(result) || (is.finite(result) && result >= 0))
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `devtools::test(filter = "max-workload")`
Expected: FAIL, `could not find function ".detect_available_memory_gib"`.

- [ ] **Step 3: Write the implementation**

Insert into `R/utilities.R` immediately after `.calibration_default_workload()` (after line 107):

```r
#' Available system memory, in GiB
#'
#' Best-effort and platform-specific. Every code path is wrapped so failure
#' -- a missing binary, a locale-mangled number, a sandbox that blocks
#' subprocess execution, an unrecognized OS -- degrades to `NA_real_` rather
#' than propagating an error. This return value feeds a default argument
#' (`rpbnb_tmb_control()`'s `max_workload`, by way of
#' `rpbnb_tmb_max_workload()`); a default argument that can error breaks
#' every caller who doesn't override it.
#' @keywords internal
#' @noRd
.detect_available_memory_gib <- function() {
  sysname <- tryCatch(Sys.info()[["sysname"]], error = function(e) NA_character_)
  if (is.na(sysname)) return(NA_real_)

  gib <- tryCatch({
    if (identical(sysname, "Linux")) {
      .detect_available_memory_gib_linux()
    } else if (identical(sysname, "Darwin")) {
      .detect_available_memory_gib_darwin()
    } else if (identical(sysname, "Windows")) {
      .detect_available_memory_gib_windows()
    } else {
      NA_real_
    }
  }, error = function(e) NA_real_)

  if (length(gib) != 1L || !is.numeric(gib) || is.na(gib) ||
      !is.finite(gib) || gib < 0) {
    return(NA_real_)
  }
  gib
}

#' @keywords internal
#' @noRd
.detect_available_memory_gib_linux <- function() {
  path <- "/proc/meminfo"
  if (!file.exists(path)) return(NA_real_)
  lines <- readLines(path, warn = FALSE)
  # MemAvailable is the kernel's own reclaimable-cache-aware estimate of what
  # can actually be handed to a new allocation; MemFree alone systematically
  # undercounts memory the kernel would reclaim on request. Fall back to
  # MemFree only on very old kernels that lack MemAvailable.
  value_kib <- .parse_meminfo_field(lines, "MemAvailable")
  if (is.na(value_kib)) value_kib <- .parse_meminfo_field(lines, "MemFree")
  if (is.na(value_kib)) return(NA_real_)
  value_kib / 1024^2
}

#' @keywords internal
#' @noRd
.parse_meminfo_field <- function(lines, field) {
  pattern <- paste0("^", field, ":\\s*([0-9]+)\\s*kB")
  hit <- grep(pattern, lines, value = TRUE)
  if (!length(hit)) return(NA_real_)
  as.numeric(sub(pattern, "\\1", hit[1L]))
}

#' @keywords internal
#' @noRd
.detect_available_memory_gib_darwin <- function() {
  vm <- tryCatch(system2("vm_stat", stdout = TRUE, stderr = FALSE),
                 error = function(e) character(0))
  if (!length(vm)) return(NA_real_)

  page_size_line <- grep("page size of", vm, value = TRUE)
  page_size <- if (length(page_size_line)) {
    as.numeric(sub(".*page size of ([0-9]+) bytes.*", "\\1", page_size_line[1L]))
  } else {
    4096  # Documented default when the header line's wording ever changes.
  }
  if (is.na(page_size) || page_size <= 0) return(NA_real_)

  free_pages <- .parse_vm_stat_field(vm, "Pages free")
  inactive_pages <- .parse_vm_stat_field(vm, "Pages inactive")
  if (is.na(free_pages) || is.na(inactive_pages)) return(NA_real_)

  (free_pages + inactive_pages) * page_size / 1024^3
}

#' @keywords internal
#' @noRd
.parse_vm_stat_field <- function(lines, field) {
  pattern <- paste0("^", field, ":\\s*([0-9]+)\\.?")
  hit <- grep(pattern, lines, value = TRUE)
  if (!length(hit)) return(NA_real_)
  as.numeric(sub(pattern, "\\1", hit[1L]))
}

#' @keywords internal
#' @noRd
.detect_available_memory_gib_windows <- function() {
  out <- tryCatch(
    system2("wmic", c("OS", "get", "FreePhysicalMemory", "/value"),
            stdout = TRUE, stderr = FALSE),
    error = function(e) character(0)
  )
  if (!length(out)) return(NA_real_)
  hit <- grep("^FreePhysicalMemory=", out, value = TRUE)
  if (!length(hit)) return(NA_real_)
  value_kib <- suppressWarnings(
    as.numeric(sub("^FreePhysicalMemory=", "", hit[1L]))
  )
  if (is.na(value_kib)) return(NA_real_)
  value_kib / 1024^2
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `devtools::test(filter = "max-workload")`
Expected: PASS, 1 test.

- [ ] **Step 5: Run the full suite**

Run: `devtools::test()`
Expected: `[ FAIL 2 | PASS 392+ ]` — the same 2 pre-existing failures, nothing new.

- [ ] **Step 6: Commit**

```bash
git add R/utilities.R tests/testthat/test-max-workload.R
git commit -m "feat: add .detect_available_memory_gib(), a never-erroring memory probe"
```

---

### Task 2: `rpbnb_tmb_max_workload()`

**Files:**
- Modify: `R/utilities.R` (append after the functions added in Task 1)
- Test: `tests/testthat/test-max-workload.R` (append)

**Interfaces:**
- Consumes: `.detect_available_memory_gib()` (Task 1), `TAPE_CALIBRATION`, `.calibration_default_workload()`.
- Produces: `rpbnb_tmb_max_workload(budget_gib = NULL, fraction = 0.8)`, exported, returning one positive numeric workload value.

- [ ] **Step 1: Write the failing tests**

Append to `tests/testthat/test-max-workload.R`:

```r
test_that("an explicit budget_gib is pure arithmetic with no fraction applied", {
  expect_equal(
    rpbnb_tmb_max_workload(budget_gib = 8),
    .calibration_default_workload()
  )
  expect_equal(
    rpbnb_tmb_max_workload(budget_gib = 16),
    signif(16 * 1024^3 / TAPE_CALIBRATION$peak_bytes_per_unit, 1)
  )
  # fraction is ignored entirely on this path -- not even applied silently.
  expect_equal(
    rpbnb_tmb_max_workload(budget_gib = 16, fraction = 0.1),
    rpbnb_tmb_max_workload(budget_gib = 16, fraction = 0.9)
  )
})

test_that("detected memory is discounted by fraction", {
  testthat::local_mocked_bindings(
    .detect_available_memory_gib = function() 10
  )
  expect_equal(
    rpbnb_tmb_max_workload(),
    signif(0.8 * 10 * 1024^3 / TAPE_CALIBRATION$peak_bytes_per_unit, 1)
  )
  expect_equal(
    rpbnb_tmb_max_workload(fraction = 0.5),
    signif(0.5 * 10 * 1024^3 / TAPE_CALIBRATION$peak_bytes_per_unit, 1)
  )
})

test_that("failed detection warns and falls back to the calibration default", {
  testthat::local_mocked_bindings(
    .detect_available_memory_gib = function() NA_real_
  )
  expect_warning(
    result <- rpbnb_tmb_max_workload(),
    "Could not detect"
  )
  expect_equal(result, .calibration_default_workload())
})

test_that("fraction is validated", {
  expect_error(rpbnb_tmb_max_workload(fraction = 0), "fraction")
  expect_error(rpbnb_tmb_max_workload(fraction = 1.5), "fraction")
  expect_error(rpbnb_tmb_max_workload(fraction = NA_real_), "fraction")
  expect_error(rpbnb_tmb_max_workload(fraction = c(0.5, 0.5)), "fraction")
})

test_that("budget_gib is validated when supplied", {
  expect_error(rpbnb_tmb_max_workload(budget_gib = "8"), "budget_gib")
  expect_error(rpbnb_tmb_max_workload(budget_gib = -1), "budget_gib")
  expect_error(rpbnb_tmb_max_workload(budget_gib = 0), "budget_gib")
  expect_error(rpbnb_tmb_max_workload(budget_gib = Inf), "budget_gib")
  expect_error(rpbnb_tmb_max_workload(budget_gib = c(8, 16)), "budget_gib")
})
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `devtools::test(filter = "max-workload")`
Expected: FAIL, `could not find function "rpbnb_tmb_max_workload"`.

- [ ] **Step 3: Write the implementation**

Append to `R/utilities.R`:

```r
#' Compute a TMB workload budget from a memory figure
#'
#' Companion to \code{rpbnb_tmb_control()}'s \code{max_workload}: rather than
#' picking a weighted-observation-draw count directly, state a memory budget
#' and let this function do the arithmetic \code{TAPE_CALIBRATION} implies.
#'
#' With \code{budget_gib} omitted, this is also \code{rpbnb_tmb_control()}'s
#' own default: every fit that doesn't set \code{max_workload} explicitly
#' already goes through this function.
#' @param budget_gib Optional memory budget in GiB, stated explicitly. When
#'   supplied, used as-is -- \code{fraction} does not apply, because a number
#'   you state yourself is not second-guessed with a discount. When omitted
#'   (the default), available memory is auto-detected and \code{fraction} of
#'   it is budgeted instead.
#' @param fraction Of auto-detected available memory, the fraction to
#'   actually budget; one number in \code{(0, 1]}. Available memory
#'   fluctuates and competes with other processes, so budgeting all of it
#'   risks the guard passing a fit that then exhausts memory anyway. Ignored
#'   when \code{budget_gib} is supplied.
#' @return One positive numeric workload value, on the same scale as
#'   \code{rpbnb_tmb_control()}'s \code{max_workload}.
#' @export
#' @examples
#' rpbnb_tmb_max_workload(budget_gib = 16)
#' \dontrun{
#' ctrl <- rpbnb_tmb_control(max_workload = rpbnb_tmb_max_workload())
#' }
rpbnb_tmb_max_workload <- function(budget_gib = NULL, fraction = 0.8) {
  if (length(fraction) != 1L || !is.numeric(fraction) || is.na(fraction) ||
      !is.finite(fraction) || fraction <= 0 || fraction > 1) {
    stop("fraction must be one number in (0, 1].", call. = FALSE)
  }

  if (!is.null(budget_gib)) {
    if (length(budget_gib) != 1L || !is.numeric(budget_gib) ||
        is.na(budget_gib) || !is.finite(budget_gib) || budget_gib <= 0) {
      stop("budget_gib must be one positive finite number, or NULL.",
           call. = FALSE)
    }
    return(signif(
      budget_gib * 1024^3 / TAPE_CALIBRATION$peak_bytes_per_unit, 1
    ))
  }

  detected_gib <- .detect_available_memory_gib()
  if (is.na(detected_gib)) {
    warning(
      "Could not detect available memory on this platform; using the ",
      "default 8 GiB calibration budget. Pass `budget_gib` explicitly to ",
      "set your own.",
      call. = FALSE
    )
    return(.calibration_default_workload())
  }

  signif(
    fraction * detected_gib * 1024^3 / TAPE_CALIBRATION$peak_bytes_per_unit, 1
  )
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `devtools::test(filter = "max-workload")`
Expected: PASS, 6 tests.

- [ ] **Step 5: Run the full suite**

Run: `devtools::test()`
Expected: `[ FAIL 2 | PASS 398+ ]`.

- [ ] **Step 6: Commit**

```bash
git add R/utilities.R tests/testthat/test-max-workload.R
git commit -m "feat: add rpbnb_tmb_max_workload()"
```

---

### Task 3: Wire the new default into `rpbnb_tmb_control()`

**Files:**
- Modify: `R/utilities.R:355-374` (the `rpbnb_tmb_control()` signature and its roxygen) and `R/utilities.R:59-97` (`.calibration_doc()`)
- Modify: `tests/testthat/test-parallel.R:210-244`

**Interfaces:**
- Consumes: `rpbnb_tmb_max_workload()` (Task 2).
- Produces: `rpbnb_tmb_control()`'s `max_workload` formal default is now `rpbnb_tmb_max_workload()` instead of `.calibration_default_workload()`.

This task changes user-visible default behavior, so its tests are written and shown failing against the *old* code first, then the implementation change makes them pass — standard red/green, just applied to an existing test file rather than a new one.

- [ ] **Step 1: Replace the two tests this change breaks**

In `tests/testthat/test-parallel.R`, replace lines 210-225 (the test named `"the default is derived from the calibration, not restated"`) with:

```r
test_that("the control default calls through to rpbnb_tmb_max_workload()", {
  # An earlier version of this test asserted the default equals the fixed
  # calibration constant directly. That stopped being the general case once
  # the default started auto-detecting memory; this test now asserts the
  # actual current contract instead, on both paths that default can take.
  testthat::local_mocked_bindings(
    .detect_available_memory_gib = function() 10
  )
  expect_identical(
    rpbnb_tmb_control()$max_workload,
    rpbnb_tmb_max_workload()
  )
})

test_that("the control default falls back to the calibration constant when detection fails", {
  testthat::local_mocked_bindings(
    .detect_available_memory_gib = function() NA_real_
  )
  expect_warning(
    result <- rpbnb_tmb_control()$max_workload,
    "Could not detect"
  )
  expect_identical(result, .calibration_default_workload())
})
```

- [ ] **Step 2: Update the documentation test to match the new prose**

In the same file, in the test named `"the documentation is generated from the calibration"` (originally lines 227-244), remove this one assertion:

```r
  expect_match(doc, format(.calibration_default_workload(),
                           scientific = FALSE, big.mark = ","), fixed = TRUE)
```

Do **not** remove it yet if Step 3 below keeps that exact formatted number in the doc text (it does — see Step 3's rewording). Instead, leave that assertion in place and add one new assertion immediately after it:

```r
  expect_match(doc, "rpbnb_tmb_max_workload", fixed = TRUE)
  expect_match(doc, "auto-detect", fixed = TRUE)
```

- [ ] **Step 3: Run the modified tests to verify they fail**

Run: `devtools::test(filter = "parallel")`
Expected: FAIL. The two rewritten tests fail because `rpbnb_tmb_control()`'s default doesn't call `rpbnb_tmb_max_workload()` yet (mocking `.detect_available_memory_gib()` has no effect on the old code path); the doc test fails because `.calibration_doc()` doesn't mention `rpbnb_tmb_max_workload` or `auto-detect` yet.

- [ ] **Step 4: Change the control default**

In `R/utilities.R`, replace:

```r
  max_workload =
    .calibration_default_workload(),
```

with:

```r
  max_workload =
    rpbnb_tmb_max_workload(),
```

(This is inside the `rpbnb_tmb_control <- function(...)` argument list, currently at `R/utilities.R:373-374`.)

- [ ] **Step 5: Extend `.calibration_doc()`'s generated text**

In `R/utilities.R`, inside `.calibration_doc()`, find this closing line of the `paste0(...)` call:

```r
    "Raise \\code{max_workload} deliberately against the memory you actually ",
    "have, not to make one particular dataset fit."
```

Replace it with:

```r
    "Raise \\code{max_workload} deliberately against the memory you actually ",
    "have, not to make one particular dataset fit.\n\n",
    "As of \\code{rpbnb_tmb_max_workload()}, the value \\code{rpbnb_tmb_control()} ",
    "actually uses by default is no longer the fixed figure above: it will ",
    "auto-detect available memory and budget 80% of it, falling back to the ",
    "fixed 8 GiB figure only when detection is unavailable on the current ",
    "platform. Call \\code{\\link{rpbnb_tmb_max_workload}} directly to set ",
    "your own budget or detection fraction."
  )
```

Note the trailing `)` moves with the new final string -- this replaces the function's closing `)` for the `paste0()` call, not just the text.

- [ ] **Step 6: Run the tests to verify they pass**

Run: `devtools::test(filter = "parallel")`
Expected: PASS, all tests including the two rewritten ones and the doc test.

Run: `devtools::test(filter = "max-workload")`
Expected: PASS, unchanged from Task 2 (this task didn't touch these tests, but confirms the wiring didn't break the direct-call contract).

- [ ] **Step 7: Run the full suite**

Run: `devtools::test()`
Expected: `[ FAIL 2 | PASS 398+ ]` — same 2 pre-existing failures. Every other test in the suite calls `rpbnb_tmb_control()` without mocking `.detect_available_memory_gib()`, so they exercise the real, unmocked auto-detect path; they must all still pass because their fits are small enough to clear even a heavily-discounted real budget. If any of them fail here, that is a genuine finding (an unexpectedly memory-starved test environment) — investigate and report it rather than mocking it away.

- [ ] **Step 8: Commit**

```bash
git add R/utilities.R tests/testthat/test-parallel.R
git commit -m "feat: make rpbnb_tmb_control()'s max_workload default memory-aware"
```

---

### Task 4: Documentation and final verification

**Files:**
- Regenerate: `NAMESPACE`, `man/rpbnb_tmb_max_workload.Rd` (new), `man/rpbnb_tmb_control.Rd`

**Interfaces:**
- Consumes: `rpbnb_tmb_max_workload()` (Task 2), the updated `rpbnb_tmb_control()` and `.calibration_doc()` (Task 3).
- Produces: no new code interfaces.

- [ ] **Step 1: Regenerate documentation**

Run:

```
& 'C:\Program Files\R\R-4.5.1\bin\Rscript.exe' -e "devtools::document()"
```

Expected: `NAMESPACE` gains `export(rpbnb_tmb_max_workload)`; `man/rpbnb_tmb_max_workload.Rd` is created; `man/rpbnb_tmb_control.Rd` picks up the new `.calibration_doc()` paragraph.

- [ ] **Step 2: Check the package builds clean**

Run:

```
& 'C:\Program Files\R\R-4.5.1\bin\Rscript.exe' -e "devtools::check(document = FALSE, args = '--no-tests', quiet = TRUE)"
```

Expected: no ERRORs and no WARNINGs from missing documentation, undeclared imports, or an unused `@noRd` conflict. NOTEs about the pre-existing `data/` layout are acceptable (see Global Constraints).

- [ ] **Step 3: Run the full suite one final time**

Run: `devtools::test()`
Expected: `[ FAIL 2 | PASS 398+ ]`.

- [ ] **Step 4: Commit**

```bash
git add man/ NAMESPACE
git commit -m "docs: regenerate NAMESPACE and man pages for rpbnb_tmb_max_workload()"
```

---

## Self-Review

**Spec coverage.** `.detect_available_memory_gib()` with per-OS branches and never-erroring contract → Task 1. `rpbnb_tmb_max_workload()`'s full behavior table (explicit budget / detected+discounted / failed-detection fallback) plus validation → Task 2. `rpbnb_tmb_control()`'s default change and the two spec-identified test rewrites (`test-parallel.R:210-225`, `227-244`) → Task 3. `.calibration_doc()`'s new paragraph → Task 3, Step 5. NAMESPACE/man regeneration → Task 4. All 7 spec test items for the new file are present in Task 1 (item 7) and Task 2 (items 1-6, renumbered as tests 1-5 in Task 2 plus the platform probe test in Task 1).

**Deliberate refinement from the spec, recorded here rather than silently done.** The spec's Testing section (item 9) said the `expect_match(doc, format(.calibration_default_workload(), ...))` assertion would be *removed*. During planning, a smaller-blast-radius option surfaced: the sentence containing that fixed number can stay factually true (it now describes the fallback/reference figure rather than "the" default), so the existing assertion can be *kept* and a new one *added* alongside it, rather than replacing it. This preserves an existing passing assertion instead of deleting it, satisfies the same underlying spec requirement (the doc text must stop *implying* the fixed figure is always what's used, and must mention `rpbnb_tmb_max_workload()`), and is reflected in Task 3, Steps 2 and 5.

**Placeholder scan.** No TBD/TODO, no "add validation" without the actual validation code, no "similar to Task N". Every code step is complete and runnable as written.

**Type consistency.** `.detect_available_memory_gib()` returns `double` (`NA_real_` or a finite non-negative value) everywhere it's referenced: Task 1's implementation and test, Task 2's mocked-binding tests, Task 3's mocked-binding tests. `rpbnb_tmb_max_workload(budget_gib = NULL, fraction = 0.8)`'s signature and both parameter names are used identically across Task 2's tests, Task 3's `rpbnb_tmb_control()` wiring, and the roxygen `@param` blocks. `TAPE_CALIBRATION$peak_bytes_per_unit` and `.calibration_default_workload()` are referenced by name consistently, never re-derived or restated as a literal.
